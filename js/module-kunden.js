// module-kunden.js
// Kundenverwaltung: Anlegen, Suchen, Bearbeiten. Noch nicht implementiert.

function initKundenModul(container) {
  container.innerHTML = '<p class="platzhalter">Modul Kunden – noch nicht implementiert.</p>';
}
