// module-teile.js
// Teilestamm inkl. Teilekatalog-Interface (LokalerKatalog / TecDocKatalog).
// Noch nicht implementiert.

function initTeileModul(container) {
  container.innerHTML = '<p class="platzhalter">Modul Teilestamm – noch nicht implementiert.</p>';
}
