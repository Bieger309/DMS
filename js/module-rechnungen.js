// module-rechnungen.js
// Rechnungen aus Aufträgen erzeugen, Zahlungsstatus, drucken.
// Noch nicht implementiert.

function initRechnungenModul(container) {
  container.innerHTML = '<p class="platzhalter">Modul Rechnungen – noch nicht implementiert.</p>';
}
