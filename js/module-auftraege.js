// module-auftraege.js
// Aufträge anlegen, Positionen erfassen, Status verwalten, drucken.
// Noch nicht implementiert.

function initAuftraegeModul(container) {
  container.innerHTML = '<p class="platzhalter">Modul Aufträge – noch nicht implementiert.</p>';
}
